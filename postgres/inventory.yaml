data_sources:
  default_source:
    jdbc_url: jdbc:postgresql://localhost:6000/postgres
    user:
      type: raw
      value: postgres
    password:
      type: raw
      value: postgres
  default_target:
    jdbc_url: jdbc:postgresql://localhost:6001/postgres
    user:
      type: raw
      value: postgres
    password:
      type: raw
      value: postgres
