data_sources:
  input:
    url: jdbc:postgresql://localhost:6000/postgres
    user:
      type: raw
      value: postgres
    password:
      type: raw
      value: postgres
  output:
    url: jdbc:postgresql://localhost:6001/postgres
    user:
      type: raw
      value: postgres
    password:
      type: raw
      value: postgres
