data_sources:
  default_source:
    url: jdbc:postgresql://localhost:6000/postgres
    user:
      type: raw
      value: postgres
    password:
      type: raw
      value: postgres
  default_target:
    url: jdbc:postgresql://localhost:6001/postgres
    user:
      type: raw
      value: postgres
    password:
      type: raw
      value: postgres
